# UI roadmap (optional)

> **Navigation only.** Some teams add an approved **UI implementation roadmap** here (e.g. `YYYYMMDD-ui-roadmap.md`) grouping screens into milestones S1, S2, …

This is **not** the Agent OS master plan (`.work/plans/full/*-full-plan.md`). Cross-link FRs and API SPECs; do not duplicate backend scope.

Create when the product needs a multi-screen UI release train beyond individual screen SPECs.
