# GitHub Actions disabled

CI workflows were removed intentionally. Run lint/build/tests locally when needed.

To stop email notifications for past runs: GitHub → repository **Settings** → **Notifications** → adjust Actions subscriptions, or unsubscribe from the failed workflow thread.
